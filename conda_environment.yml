name: demiurge
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - pandas
  - rdkit
  # Full, pinned JDK: both java and javac are deployment requirements.
  - openjdk=23.0.2
  - pip

# OpenBabel is intentionally absent: it is not part of the production
# SPECTRAPRINTS_NMR_V2 preparation contract.
